# High Consulting AI — Fixed Starter

Steps:
1. Copy this project into your repo root (replace files).
2. Copy `.env.template` to `.env.local` and fill values.
3. Install deps: `npm install`
4. Run dev: `npm run dev` or build: `npm run build`
5. Deploy to Render (connect GitHub repo).

Notes:
- Supabase keys must use the NEXT_PUBLIC_ prefix for client use.
- ReactFlow dynamic imports are set up for SSR safety.
