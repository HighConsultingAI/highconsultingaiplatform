/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  experimental: {
    // keep defaults; you can remove experimental keys if unused
  }
}
module.exports = nextConfig
